/** Max lead IDs accepted per verify API request. */
export const MILLION_VERIFIER_MAX_BATCH = 50;

/** Concurrent Million Verifier Real-Time API calls per batch. */
export const MILLION_VERIFIER_CONCURRENCY = 5;
